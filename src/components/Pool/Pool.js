import React from 'react';
import './Pool.css';
import { withTranslation } from 'react-i18next';

function Pool(){
    return(
        <div>
            Pool
        </div>
    )
}

export default withTranslation()(Pool);
